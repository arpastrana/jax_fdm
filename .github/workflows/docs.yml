name: docs

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main
  workflow_dispatch:

# Cancel a PR's in-flight docs build when new commits are pushed to it. Every
# other event gets a run-unique group, so a stuck run never holds up a deploy.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.event_name == 'pull_request' && 'pr' || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  JAX_PLATFORMS: cpu
  # The sync step decides the environment; a run step must not re-sync default groups.
  UV_NO_SYNC: "1"

jobs:
  build:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up uv
        uses: astral-sh/setup-uv@v10
        with:
          enable-cache: true

      # PySide6 (via compas_viewer) links against system GL libraries that the
      # headless Linux runners do not ship.
      - name: Install GL libraries
        run: sudo apt-get update && sudo apt-get install -y libegl1 libopengl0

      - name: Install package
        run: uv sync --locked --no-default-groups --group docs --extra viz

      - name: Build docs
        run: uv run mkdocs build --strict

  deploy:
    if: github.event_name != 'pull_request'
    runs-on: ubuntu-latest
    # Serialize gh-pages writes, as concurrent mike deploys race on the branch.
    concurrency:
      group: ${{ github.workflow }}-deploy
      cancel-in-progress: false
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          # mike reads and commits the gh-pages branch.
          fetch-depth: 0

      - name: Configure git credentials
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com

      - name: Set up uv
        uses: astral-sh/setup-uv@v10
        with:
          enable-cache: true

      - name: Install GL libraries
        run: sudo apt-get update && sudo apt-get install -y libegl1 libopengl0

      - name: Install package
        run: uv sync --locked --no-default-groups --group docs --extra viz

      # Pushes to main update the rolling "dev" docs; a release tag vX.Y.Z
      # publishes the "X.Y.Z" version and points the "latest" alias (the
      # site's default landing version) at it.
      - name: Deploy dev docs
        if: github.ref == 'refs/heads/main'
        run: uv run mike deploy --push --update-aliases dev

      - name: Deploy release docs
        if: startsWith(github.ref, 'refs/tags/v')
        run: |
          VERSION=${GITHUB_REF#refs/tags/v}
          uv run mike deploy --push --update-aliases "$VERSION" latest
          uv run mike set-default --push latest
